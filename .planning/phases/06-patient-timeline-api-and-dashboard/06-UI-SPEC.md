---
phase: 6
slug: patient-timeline-api-and-dashboard
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-08
---

# Phase 6 — UI Design Contract

> Visual and interaction contract for the Patient Timeline API and Dashboard phase.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none (manual Tailwind utility classes) |
| Icon library | lucide-react (already used throughout: Stethoscope, Pill, AlertTriangle, Brain, Loader2, RefreshCw, ChevronRight, FlaskConical, Activity) |
| Font | Inter (Google Fonts, loaded via `next/font/google` in layout.tsx) |

Source: detected from `frontend/app/layout.tsx`, `frontend/components/dashboard/PatientSummary.tsx`, `frontend/tailwind.config.ts`, `frontend/app/globals.css`.

No `components.json` found. shadcn gate: project is Next.js but existing UI is hand-written Tailwind without shadcn — consistent with all prior phases. Do NOT introduce shadcn into this phase.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps (`gap-1`), bullet dot margin |
| sm | 8px | Compact element spacing, icon padding (`p-1.5`) |
| md | 16px | Default element spacing, card padding (`p-4`) |
| xl | 24px | Section gaps (`space-y-6`), grid gaps (`gap-4`) |
| 2xl | 32px | Major section breaks (`space-y-8`) |
| 3xl | 48px | Page-level spacing (empty state `py-12`) |

Note: The existing Section card component uses Tailwind `p-5` (20px) — this is a pre-existing one-off in the codebase, not a new spacing token. Do not introduce `lg = 20px` as a scale token. New components must use `p-4` (16px) or `p-6` (24px) for card body padding.

Exceptions: Touch targets for the "Atualizar dados" refresh button must be minimum 44px tall (keyboard/pointer accessibility) — use `py-2.5` minimum on the button.

Source: derived from existing `dashboard/page.tsx` and `PatientSummary.tsx` class patterns.

---

## Typography

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Label | 12px (`text-xs`) | 400 (regular) | 1.4 | Metadata, dates, units, flags, document count, disclaimer text |
| Body | 14px (`text-sm`) | 400 (regular) | 1.5 (`leading-relaxed`) | List items, observation values, medication names |
| Heading | 14px (`text-sm`) | 600 (semibold) | 1.2 | Section titles (`font-semibold text-gray-800`) |
| Display | 24px (`text-2xl`) | 600 (semibold) | 1.2 | Page title (`font-semibold text-gray-900`) |

Weights: exactly 2 — 400 (regular) and 600 (semibold, `font-semibold`). Do not use `font-bold` (700) anywhere in this phase.

Note: 14px heading and 14px body are differentiated by weight, not size — consistent with existing codebase pattern. Do not add a 4th size.

Source: pre-populated from `dashboard/page.tsx` and `PatientSummary.tsx` observed class usage. Display weight updated from 700 to 600 per checker revision.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#ffffff` / `bg-white` | Card backgrounds, header background, main content surfaces |
| Secondary (30%) | `#f9fafb` / `bg-gray-50` | Page background (`body` level), section backgrounds |
| Accent (10%) | `#2563eb` / `blue-600` | Primary CTA button fill, active nav icon, UHR logo icon, upload document link, "Enviar documentos" CTA |
| Destructive | `#dc2626` / `red-600` | Emergency quick-link icon only — no destructive actions exist in this phase |

Accent reserved for: primary CTA button ("Enviar documentos"), UHR logo icon in nav, the "Emergência" quick-link icon. NOT used on informational section icons (those use semantic tints: blue-50/blue-600 for conditions, green-50/green-600 for medications, red-50/red-600 for allergies, amber-50/amber-800 for warnings, purple-50/purple-600 for AI analysis).

Semantic tint palette (for structured data section — matches existing PatientSummary.tsx icon colors):

| Section | Icon Color | Background Tint | Text |
|---------|-----------|-----------------|------|
| Condições ativas | `text-blue-600` | `bg-blue-50` | `text-blue-900` / `text-blue-800` |
| Medicamentos | `text-green-600` | `bg-green-50` | `text-gray-700` |
| Alergias | `text-red-600` | `bg-red-50` | `text-gray-700` |
| Exames / Labs | `text-teal-600` | `bg-teal-50` | `text-gray-700` |
| Aviso / needs_review | `text-amber-700` | `bg-amber-50` | `text-amber-800` |

Source: directly extracted from `PatientSummary.tsx` Section component usage.

---

## Focal Point

Primary screen: the dashboard page after this phase contains two stacked sections — "Análise do Histórico" (existing LLM panel) and "Dados Clínicos" (new structured data panel).

Focal point: the "Dados Clínicos" section heading and its 2-column grid of Section cards. This is the primary new visual element the user's eye should land on when scrolling past the analysis panel. It is positioned immediately below the existing `<PatientSummary />` block, separated by `space-y-8` (32px). The section heading uses the Display style (`font-semibold text-2xl text-gray-900`). No hero image, illustration, or decorative element — the card grid itself is the focal anchor.

---

## Component Inventory

Reuse these exact existing patterns — do not invent new structural components:

### Section card (existing, reuse)
```
<div className="rounded-xl border border-gray-200 bg-white p-5">
  <div className="mb-3 flex items-center gap-2">
    <div className={`rounded-lg p-1.5 ${color}`}><Icon className="h-4 w-4" /></div>
    <h3 className="text-sm font-semibold text-gray-800">{title}</h3>
  </div>
  {children}
</div>
```
Used by: PatientSummary.tsx `Section` component. New structured data section must import and reuse this `Section` component. The `p-5` on this existing component is preserved as-is — do not change existing component markup.

### Loading state (existing, reuse)
```
<div className="flex items-center justify-center gap-2 py-12 text-sm text-gray-500">
  <Loader2 className="h-4 w-4 animate-spin" />
  {loadingText}
</div>
```

### Empty/error state with CTA (existing, reuse)
```
<div className="rounded-xl border border-gray-200 bg-white p-8 text-center">
  <FileText className="mx-auto mb-3 h-10 w-10 text-gray-300" />
  <p className="text-sm font-medium text-gray-700">{heading}</p>
  <p className="mt-1 text-xs text-gray-500">{body}</p>
  <Link href="/upload" className="mt-4 inline-flex items-center gap-1.5 rounded-lg bg-blue-600 px-4 py-2 text-sm font-medium text-white hover:bg-blue-700 transition-colors">
    {cta} <ChevronRight className="h-4 w-4" />
  </Link>
</div>
```

### List item (existing, reuse)
```
<li className="flex items-center gap-2 text-sm text-gray-700">
  <span className="h-1.5 w-1.5 shrink-0 rounded-full bg-{color}-400" />
  {text}
</li>
```

### New: Observation row (lab value with flag)
```
<li className="flex items-center justify-between gap-2 py-1">
  <span className="text-sm text-gray-700">{analyte_norm}</span>
  <div className="flex items-center gap-2">
    <span className="text-sm text-gray-900 font-medium">{value_num ?? value_str} {unit}</span>
    {flag && <span className={`rounded px-1.5 py-0.5 text-[10px] font-semibold ${flagColor}`}>{flag}</span>}
  </div>
</li>
```
Flag color map: `H/HH` → `bg-red-100 text-red-700`; `L/LL` → `bg-blue-100 text-blue-700`; normal → omit badge.

### New: needs_review inline indicator
When `needs_review=true` on an observation, append a small amber dot: `<span className="h-1.5 w-1.5 rounded-full bg-amber-400" title="Valor com conflito de deduplicação — pode requerer revisão" />`. Do not show a modal or blocking UI — soft indicator only.

### New: PatientStructuredData component
New client component `frontend/components/dashboard/PatientStructuredData.tsx`. Calls `GET /patient/summary`. Renders four Section cards in a `grid gap-4 sm:grid-cols-2`: condições, medicamentos, alergias, últimos exames. Renders below the existing `<PatientSummary />` block in `dashboard/page.tsx`.

---

## Copywriting Contract

All copy is in Brazilian Portuguese, consistent with existing UI. No English strings in user-visible text.

| Element | Copy |
|---------|------|
| Primary CTA (empty state) | "Enviar documentos" |
| New section heading | "Dados Clínicos" |
| New section subheading (page.tsx) | "Informações estruturadas extraídas dos documentos." |
| Loading state — PatientStructuredData | "Carregando dados clínicos..." |
| Empty state heading | "Nenhum dado clínico encontrado" |
| Empty state body | "Faça upload de documentos médicos para visualizar condições, medicamentos e exames." |
| Error state | "Erro ao carregar dados clínicos. Tente novamente." |
| Condições section title | "Condições Ativas" |
| Medicamentos section title | "Medicamentos em Uso" |
| Alergias section title | "Alergias Conhecidas" |
| Exames section title | "Últimos Exames" |
| Empty list — condições | "Nenhuma condição registrada." |
| Empty list — medicamentos | "Nenhum medicamento registrado." |
| Empty list — alergias | "Nenhuma alergia registrada." |
| Empty list — exames | "Nenhum exame registrado." |
| needs_review tooltip | "Valor com conflito de deduplicação — pode requerer revisão" |
| Refresh button | "Atualizar dados" |
| Destructive confirmation | none — no destructive actions in this phase |

Source: language style derived from existing Brazilian Portuguese strings in `PatientSummary.tsx` and `dashboard/page.tsx`. "Atualizar" updated to "Atualizar dados" per checker recommendation.

---

## Interaction Contract

### New PatientStructuredData section

1. Renders below the existing "Análise do Histórico" section in `dashboard/page.tsx` — additive, not replacing (CONTEXT.md D-01).
2. Fetches `GET /patient/summary` on mount via `useEffect`. Shows loading spinner during fetch. Shows empty state if response has zero items across all categories. Shows error state if fetch throws.
3. Refresh button (text link, not primary button) triggers re-fetch. Uses existing `RefreshCw` icon at `h-3 w-3`, `text-xs text-gray-400 hover:text-gray-600` — matches existing PatientSummary refresh button pattern exactly. Label: "Atualizar dados".
4. No pagination in Phase 6. All active conditions, current medications, allergies, and latest labs per analyte render inline. Volume is expected to be small (personal health record — single user).
5. Observation items with `flag = "H" | "HH"` render with red tint badge. Items with `flag = "L" | "LL"` render with blue tint badge. Items with `flag = null` or `"normal"` render no badge.
6. Items with `needs_review = true` render an amber dot indicator (no blocking UI).
7. No animation beyond the existing `animate-spin` on Loader2. No transitions beyond Tailwind `transition-colors` on interactive elements.

### Timeline API (backend — no new frontend UI in Phase 6)

Timeline endpoints (`GET /timeline/observations/{analyte}`, `GET /timeline/conditions`, `GET /timeline/medications`) are backend-only in this phase. No chart or list view for the timeline is rendered in the dashboard. The response shape is defined rich (CONTEXT.md D-04) to enable Phase 7+ visualization without an API change. Frontend consumers of timeline endpoints are deferred (CONTEXT.md Deferred: VIZ-01).

### Emergency profile page (existing, refactored backend only)

The emergency profile frontend (`frontend/app/emergency/`) is NOT modified in this phase. The `services/emergency.py` backend refactor changes the data source only — not the `EmergencyProfile` schema or the frontend rendering (CONTEXT.md D-11).

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — shadcn not initialized |
| third-party | none | not applicable |

No new component libraries or registries are introduced. All UI is built from existing Tailwind utility classes and Lucide icons already present in the project.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

---

*Phase: 06-patient-timeline-api-and-dashboard*
*UI-SPEC authored: 2026-04-08*
*UI-SPEC revised: 2026-04-08 — checker fixes: collapsed typography to 2 weights (dropped 700, display now uses 600); removed non-standard lg=20px spacing token; updated refresh button copy to "Atualizar dados"; added focal point declaration.*
*Sources: CONTEXT.md decisions D-01 through D-12; existing PatientSummary.tsx; dashboard/page.tsx; layout.tsx; globals.css; tailwind.config.ts; lib/api.ts*
