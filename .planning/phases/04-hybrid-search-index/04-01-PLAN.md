---
phase: 04-hybrid-search-index
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/rag/__init__.py
  - backend/tests/rag/test_search_schema.py
  - backend/tests/rag/test_context_block.py
  - backend/tests/rag/test_embeddings.py
  - backend/tests/rag/test_indexer.py
  - backend/tests/rag/test_retriever.py
autonomous: false
requirements:
  - RETRIEVE-01
  - RETRIEVE-02
  - RETRIEVE-03

must_haves:
  truths:
    - "Wave 0 test stubs exist for every testable module in Phase 4"
    - "All test files are importable and discoverable by pytest"
    - "Each test file has at least one test function that currently fails (xfail or skip) because the code under test does not yet exist"
  artifacts:
    - path: "backend/tests/rag/__init__.py"
      provides: "Package init for test discovery"
    - path: "backend/tests/rag/test_search_schema.py"
      provides: "Test stubs for RETRIEVE-01 index schema"
      contains: "def test_"
    - path: "backend/tests/rag/test_context_block.py"
      provides: "Test stubs for RETRIEVE-02 context block assembly"
      contains: "def test_"
    - path: "backend/tests/rag/test_embeddings.py"
      provides: "Test stubs for RETRIEVE-02 embedding generation"
      contains: "def test_"
    - path: "backend/tests/rag/test_indexer.py"
      provides: "Test stubs for RETRIEVE-02 indexer update"
      contains: "def test_"
    - path: "backend/tests/rag/test_retriever.py"
      provides: "Test stubs for RETRIEVE-03 hybrid query"
      contains: "def test_"
  key_links: []
---

<objective>
Create Wave 0 test stubs for all Phase 4 modules AND verify the Azure AI Search tier supports vector fields.

Purpose: Test-first development — all subsequent plans have test targets to turn green. Tier verification ensures we do not hit a wall at index creation time.
Output: 6 test files in `backend/tests/rag/`, tier verification documented.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-hybrid-search-index/04-CONTEXT.md
@.planning/phases/04-hybrid-search-index/04-RESEARCH.md
@.planning/phases/04-hybrid-search-index/04-VALIDATION.md

@backend/models/document.py
@backend/services/azure/search.py
@backend/services/rag/indexer.py
@backend/services/rag/retriever.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Wave 0 test stubs for all Phase 4 modules</name>
  <files>
    backend/tests/rag/__init__.py
    backend/tests/rag/test_search_schema.py
    backend/tests/rag/test_context_block.py
    backend/tests/rag/test_embeddings.py
    backend/tests/rag/test_indexer.py
    backend/tests/rag/test_retriever.py
  </files>
  <read_first>
    backend/tests/extraction/test_classifier.py
    backend/models/document.py
    backend/services/azure/search.py
    backend/services/rag/indexer.py
    backend/services/rag/retriever.py
  </read_first>
  <action>
Create `backend/tests/rag/__init__.py` (empty file).

Create `backend/tests/rag/test_search_schema.py`:
- Use `sys.path.insert(0, os.path.join(os.path.dirname(__file__), "../.."))` at the top (same pattern as `test_classifier.py`).
- `test_index_fields_include_content_vector()` — will assert that the index schema builder returns a field named `content_vector` with `vector_search_dimensions=1536`. Mark `@pytest.mark.skip(reason="schema builder not implemented yet")`.
- `test_index_has_filterable_document_family()` — will assert `document_family` field is filterable. Skip.
- `test_index_has_filterable_collection_date()` — will assert `collection_date` field is filterable. Skip.
- `test_index_has_semantic_config_uhr_semantic()` — will assert semantic config named `uhr-semantic` exists. Skip.
- `test_vector_search_profile_uses_snake_case()` — will assert profile uses `algorithm_configuration_name` (not camelCase). Skip.

Create `backend/tests/rag/test_context_block.py`:
- `test_structured_lab_block_includes_findings()` — given a `StructuredResult` with `document_family="structured_lab"` and `structured_data` containing a finding `{"name": "Hemoglobina", "value": "12.5", "unit": "g/dL", "flag": "normal"}`, assert the output contains `"Hemoglobina: 12.5 g/dL [normal]"`. Skip.
- `test_imaging_block_includes_impression()` — given `document_family="imaging_narrative"` with `structured_data={"impression": "Normal chest X-ray"}`, assert output contains `"Normal chest X-ray"`. Skip.
- `test_clinical_note_includes_diagnoses()` — given `document_family="clinical_narrative"` with `structured_data={"diagnoses": ["Hipertensao"]}`, assert output contains `"Hipertensao"`. Skip.
- `test_medication_block_includes_drug_names()` — given `document_family="medication_document"` with `structured_data={"medications": [{"name": "Losartana", "dose": "50mg"}]}`, assert output contains `"Losartana 50mg"`. Skip.
- `test_null_structured_result_falls_back_to_anonymized_text()` — given `sr=None` and `anonymized_text="x" * 2000`, assert output equals first 1000 chars. Skip.
- `test_empty_structured_result_falls_back()` — given all fields empty/missing, assert fallback to anonymized_text[:1000]. Skip.
- `test_block_truncated_at_2000_chars()` — given very long content, assert `len(result) <= 2000`. Skip.

Create `backend/tests/rag/test_embeddings.py`:
- `test_generate_embedding_returns_list_of_floats()` — mock the OpenAI client, assert return is `list[float]` of length 1536. Skip.
- `test_generate_embedding_returns_none_on_failure()` — mock the client to raise `Exception`, assert return is `None`. Skip.
- `test_generate_embedding_skips_in_mock_mode()` — patch `_use_mock` to return True, assert returns `None` without calling OpenAI. Skip.

Create `backend/tests/rag/test_indexer.py`:
- `test_index_after_upload_calls_generate_embedding()` — mock `generate_embedding` and `index_document`, call updated `index_after_upload`, assert `generate_embedding` was called. Skip.
- `test_index_after_upload_passes_vector_to_index_document()` — mock embedding return as `[0.1]*1536`, assert `index_document` received `content_vector=[0.1]*1536`. Skip.
- `test_index_after_upload_works_without_embedding()` — mock embedding return as `None`, assert `index_document` still called (without vector). Skip.

Create `backend/tests/rag/test_retriever.py`:
- `test_build_context_prompt_calls_generate_embedding()` — mock `generate_embedding` and `search`, assert embedding called with the query text. Skip.
- `test_hybrid_search_passes_vectorized_query()` — mock the search client, assert `vector_queries` parameter is passed. Skip.
- `test_retriever_works_in_mock_mode()` — with `USE_MOCK_AZURE=true`, call `build_context_prompt`, assert no embedding call and mock search used. Skip.

All test functions use `@pytest.mark.skip(reason="Wave 0 stub — implementation in Plan XX")` where XX is the plan number that will implement the feature.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/rag/ --collect-only -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - backend/tests/rag/__init__.py exists (empty file)
    - backend/tests/rag/test_search_schema.py contains "def test_index_fields_include_content_vector"
    - backend/tests/rag/test_search_schema.py contains "def test_vector_search_profile_uses_snake_case"
    - backend/tests/rag/test_context_block.py contains "def test_structured_lab_block_includes_findings"
    - backend/tests/rag/test_context_block.py contains "def test_null_structured_result_falls_back_to_anonymized_text"
    - backend/tests/rag/test_context_block.py contains "def test_block_truncated_at_2000_chars"
    - backend/tests/rag/test_embeddings.py contains "def test_generate_embedding_returns_list_of_floats"
    - backend/tests/rag/test_embeddings.py contains "def test_generate_embedding_skips_in_mock_mode"
    - backend/tests/rag/test_indexer.py contains "def test_index_after_upload_calls_generate_embedding"
    - backend/tests/rag/test_retriever.py contains "def test_hybrid_search_passes_vectorized_query"
    - `cd backend && python -m pytest tests/rag/ --collect-only -q` reports at least 20 tests collected
    - All test files use `sys.path.insert(0, os.path.join(os.path.dirname(__file__), "../.."))` at the top
    - All test functions are marked with `@pytest.mark.skip`
  </acceptance_criteria>
  <done>All 6 test files created in backend/tests/rag/. pytest --collect-only discovers all test functions. All marked skip (Wave 0 stubs).</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: Verify Azure AI Search tier supports vector fields</name>
  <files>N/A</files>
  <action>
User must verify Azure infrastructure readiness in the Azure Portal:

1. Azure AI Search tier check:
   - Go to Azure Portal -> your Azure AI Search service -> Overview
   - Check the "Pricing tier" field
   - If Basic or Standard (S1/S2/S3): vector search IS supported (confirmed for services created after April 2024)
   - If Free (F1): vector search is NOT supported — must upgrade to at least Basic
   - Note the service creation date (visible in Properties)

2. Azure OpenAI embedding deployment check:
   - Go to Azure Portal -> Azure OpenAI -> Deployments
   - Confirm `text-embedding-3-small` deployment exists
   - If not: create it via Model Deployments -> Deploy Model -> text-embedding-3-small
   - The deployment name must match the `AZURE_OPENAI_EMBEDDING_DEPLOYMENT` env var

3. Set the env var `AZURE_OPENAI_EMBEDDING_DEPLOYMENT` in `.env` to the deployment name.
  </action>
  <verify>
    <automated>cd backend && python -c "import os; from dotenv import load_dotenv; load_dotenv(); assert os.environ.get('AZURE_OPENAI_EMBEDDING_DEPLOYMENT'), 'AZURE_OPENAI_EMBEDDING_DEPLOYMENT not set'; print('env var ok')"</automated>
  </verify>
  <acceptance_criteria>
    - User confirms Azure AI Search tier is Basic or higher
    - User confirms text-embedding-3-small deployment exists in Azure OpenAI
    - AZURE_OPENAI_EMBEDDING_DEPLOYMENT env var is set in .env
    - `cd backend && python -c "import os; from dotenv import load_dotenv; load_dotenv(); assert os.environ.get('AZURE_OPENAI_EMBEDDING_DEPLOYMENT')"` exits 0
  </acceptance_criteria>
  <done>Azure AI Search tier verified as supporting vector search. Azure OpenAI embedding deployment confirmed. AZURE_OPENAI_EMBEDDING_DEPLOYMENT env var set.</done>
</task>

</tasks>

<verification>
- `cd backend && python -m pytest tests/rag/ --collect-only -q` discovers all Wave 0 stubs
- Azure tier confirmed to support vector search
- Azure OpenAI embedding deployment confirmed
</verification>

<success_criteria>
- All 6 test files exist and are discoverable by pytest
- Azure AI Search tier documented as supporting vector fields
- Azure OpenAI text-embedding-3-small deployment confirmed
</success_criteria>

<output>
After completion, create `.planning/phases/04-hybrid-search-index/04-01-SUMMARY.md`
</output>
