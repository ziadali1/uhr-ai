---
phase: 05-query-routing-and-context-assembly
plan: 04
type: execute
wave: 2
depends_on:
  - 05-02
  - 05-03
files_modified:
  - backend/services/rag/retriever.py
autonomous: true
requirements:
  - RETRIEVE-04
  - RETRIEVE-05

must_haves:
  truths:
    - "build_context_prompt() signature is unchanged: (question: str, user_id: str) -> tuple[str, list[str]]"
    - "build_context_prompt() calls route_query() to classify intent before any search or SQL"
    - "SQL results appear before retrieved documents in the final context (D-12/D-13 ordering)"
    - "Retrieved documents are formatted via assemble_chat_block(), not raw r.excerpt"
    - "SYSTEM_PROMPT instructs Claude to treat === DADOS ESTRUTURADOS === as ground truth (D-18)"
    - "search top_k reduced from 3 to 2 when SQL block is non-empty (D-14)"
    - "api/chat.py requires zero changes — build_context_prompt signature preserved"
  artifacts:
    - path: "backend/services/rag/retriever.py"
      provides: "refactored build_context_prompt() with router integration"
      contains: "from services.rag.router import route_query"
  key_links:
    - from: "backend/services/rag/retriever.py build_context_prompt()"
      to: "services.rag.router.route_query"
      via: "direct import and call"
      pattern: "from services.rag.router import route_query"
    - from: "backend/services/rag/retriever.py build_context_prompt()"
      to: "services.rag.router.assemble_chat_block"
      via: "direct import and call per search result"
      pattern: "assemble_chat_block"
    - from: "backend/services/rag/retriever.py build_context_prompt()"
      to: "services.rag.router.execute_sql_steps"
      via: "direct import and call when intent is sql_only or mixed"
      pattern: "execute_sql_steps"
---

<objective>
Refactor `backend/services/rag/retriever.py` internals to call the router as the entry point. Replace the flat BM25+excerpt pipeline with: route_query() → execute SQL steps → hybrid search → assemble_chat_block() per result → merge sections. Update SYSTEM_PROMPT per D-18/D-19.

Purpose: Wire Plans 02/03 into the actual chat pipeline. api/chat.py calls build_context_prompt() — this refactor makes it use the router without changing the external signature.
Output: refactored retriever.py where build_context_prompt() orchestrates routing, SQL, search, and context assembly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-query-routing-and-context-assembly/05-CONTEXT.md
@.planning/phases/05-query-routing-and-context-assembly/05-02-SUMMARY.md
@.planning/phases/05-query-routing-and-context-assembly/05-03-SUMMARY.md

<interfaces>
Current retriever.py (full content — 51 lines):
  SYSTEM_PROMPT: 6-rule string in Brazilian Portuguese
  build_context_prompt(question: str, user_id: str) -> tuple[str, list[str]]:
    - generates embedding
    - calls search(question, user_id, top_k=3, query_vector=vector)
    - formats each result as "[Fonte: {r.source_name}]\n{r.excerpt}"
    - returns (f"{SYSTEM_PROMPT}\n\n=== DOCUMENTOS DO PACIENTE ===\n\n{context_block}\n\n=== FIM DOS DOCUMENTOS ===", sources)

New router.py exports (from Plans 02/03):
  route_query(question: str, user_id: str) -> RoutingResult
    RoutingResult.intent: "sql_only" | "search_only" | "mixed"
    RoutingResult.sql_steps: list[str]
    RoutingResult.search_queries: list[str]
  execute_sql_steps(sql_steps: list[str], user_id: str) -> list[dict]
  format_sql_block(sql_results: list[dict]) -> str  (returns "" if empty)
  assemble_chat_block(doc_id: str, source_name: str, excerpt: str, collection_date: str | None) -> str

search() from azure/search.py:
  search(query: str, user_id: str, top_k: int = 3, query_vector: list[float] | None = None) -> list[SearchResult]
  SearchResult: doc_id (str), source_name (str), excerpt (str), score (float)

D-18/D-19 SYSTEM_PROMPT update: add instructions to treat === DADOS ESTRUTURADOS === as ground truth,
  use documents as supporting evidence, synthesize across both.
  Preserve all 6 existing rules: cite sources, never invent, never diagnose, Brazilian Portuguese.

D-14: reduce top_k 3 -> 2 when sql_block is non-empty.
D-12/D-13 context ordering: SQL block first, then documents block.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SYSTEM_PROMPT and refactor build_context_prompt internals</name>
  <files>backend/services/rag/retriever.py</files>
  <read_first>
    - backend/services/rag/retriever.py (MANDATORY — read full current content before touching anything)
    - backend/services/rag/router.py (verify exports: route_query, execute_sql_steps, format_sql_block, assemble_chat_block)
    - backend/api/chat.py (confirm build_context_prompt call site — verify signature must not change)
    - .planning/phases/05-query-routing-and-context-assembly/05-CONTEXT.md (D-12, D-13, D-14, D-18, D-19)
  </read_first>
  <action>
Read backend/services/rag/retriever.py in full before making any change.

REPLACE the entire file content with the following (the file is 51 lines — full rewrite is appropriate):

"""
RAG Retriever — routes query, fetches SQL + search results, assembles context for Claude.

Refactored in Phase 5 (RETRIEVE-04, RETRIEVE-05):
  - Calls route_query() to classify intent before retrieval
  - Executes SQL steps for structured patient data (sql_only, mixed)
  - Calls hybrid search for narrative queries (search_only, mixed)
  - Assembles structured clinical context blocks via assemble_chat_block()
  - Preserves build_context_prompt(question, user_id) -> tuple[str, list[str]] signature
"""
from services.azure.search import search, SearchResult
from services.azure.embeddings import generate_embedding
from services.rag.router import (
    route_query,
    execute_sql_steps,
    format_sql_block,
    assemble_chat_block,
)

SYSTEM_PROMPT = """Você é um assistente médico de suporte ao paciente.
Seu papel é responder perguntas com base EXCLUSIVAMENTE nas informações fornecidas abaixo.
Regras obrigatórias:
- Nunca invente informações que não estejam nos dados fornecidos.
- Sempre cite a fonte (nome do documento) ao mencionar uma informação de documento.
- Se a informação não estiver disponível, diga claramente que não encontrou.
- Nunca emita diagnósticos. Se perguntado, lembre que suas respostas são informativas e não substituem avaliação médica.
- Responda sempre em português do Brasil.
- Os dados estruturados do paciente (seção === DADOS ESTRUTURADOS DO PACIENTE ===) representam fatos verificados extraídos dos registros médicos. Trate-os como fonte primária de verdade.
- Os documentos de suporte (seção === DOCUMENTOS DE SUPORTE ===) fornecem contexto narrativo e clínico adicional. Use-os como evidência de apoio.
- Ao responder, sintetize as informações de ambas as seções quando disponíveis."""


def build_context_prompt(question: str, user_id: str) -> tuple[str, list[str]]:
    """
    Route query, fetch SQL + search results, assemble structured context for Claude.

    Signature preserved: (question: str, user_id: str) -> (system_prompt_with_context, sources).
    api/chat.py requires no changes.
    """
    # Step 1: classify intent, extract entities, resolve temporal references (RETRIEVE-04)
    routing = route_query(question, user_id)

    # Step 2: execute SQL steps for structured patient data (sql_only or mixed)
    sql_block = ""
    if routing.intent in ("sql_only", "mixed") and routing.sql_steps:
        sql_results = execute_sql_steps(routing.sql_steps, user_id)
        sql_block = format_sql_block(sql_results)

    # Step 3: hybrid search for narrative queries (search_only or mixed)
    # Reduce top_k 3->2 when SQL block has data (D-14 context budget)
    search_results: list[SearchResult] = []
    sources: list[str] = []
    if routing.intent in ("search_only", "mixed"):
        top_k = 2 if sql_block else 3
        # Use first search_query if available, else fall back to original question
        search_query = routing.search_queries[0] if routing.search_queries else question
        query_vector = generate_embedding(search_query)
        search_results = search(search_query, user_id, top_k=top_k, query_vector=query_vector)
        for r in search_results:
            if r.source_name not in sources:
                sources.append(r.source_name)

    # sql_only: no search — sources come from SQL table labels
    if routing.intent == "sql_only" and not sources:
        sources = ["dados estruturados do paciente"]

    # Step 4: assemble context sections (D-12/D-13 ordering: SQL first, docs second)
    context_sections: list[str] = []

    if sql_block:
        context_sections.append(sql_block)

    if search_results:
        doc_parts: list[str] = []
        for r in search_results:
            block = assemble_chat_block(
                doc_id=r.doc_id,
                source_name=r.source_name,
                excerpt=r.excerpt,
                collection_date=None,  # not available on SearchResult; assembler uses "data desconhecida"
            )
            doc_parts.append(block)
        docs_section = (
            "=== DOCUMENTOS DE SUPORTE ===\n\n"
            + "\n\n---\n\n".join(doc_parts)
            + "\n\n=== FIM DOS DOCUMENTOS ==="
        )
        context_sections.append(docs_section)

    if not context_sections:
        return f"{SYSTEM_PROMPT}\n\nNenhum dado médico encontrado para este usuário.", []

    full_context = "\n\n".join(context_sections)
    full_system = f"{SYSTEM_PROMPT}\n\n{full_context}"

    return full_system, sources
  </action>
  <verify>
    <automated>cd /c/Users/zezis/Desktop/code/uhr/uhr-ai/backend && python -m pytest tests/rag/test_retriever.py -v -q 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - backend/services/rag/retriever.py contains "from services.rag.router import route_query" (grep-verifiable)
    - backend/services/rag/retriever.py contains "assemble_chat_block" (grep-verifiable)
    - backend/services/rag/retriever.py contains "execute_sql_steps" (grep-verifiable)
    - backend/services/rag/retriever.py contains "format_sql_block" (grep-verifiable)
    - backend/services/rag/retriever.py contains "DADOS ESTRUTURADOS DO PACIENTE" in SYSTEM_PROMPT (grep-verifiable — D-18)
    - backend/services/rag/retriever.py contains "DOCUMENTOS DE SUPORTE" (grep-verifiable — D-13)
    - build_context_prompt signature is "def build_context_prompt(question: str, user_id: str)" (grep-verifiable — signature unchanged)
    - Running "python -m pytest tests/rag/test_retriever.py -v" shows 3 PASSED + 1 SKIPPED (existing 3 tests green, new stub still skipped)
    - cd backend && python -c "from services.rag.retriever import build_context_prompt; r = build_context_prompt('test', 'u1'); print(type(r), len(r))" exits 0
  </acceptance_criteria>
  <done>retriever.py refactored. build_context_prompt() calls route_query(), execute_sql_steps(), assemble_chat_block(). Signature unchanged. Existing 3 tests remain green.</done>
</task>

</tasks>

<verification>
Run full rag test suite:
  cd /c/Users/zezis/Desktop/code/uhr/uhr-ai/backend && python -m pytest tests/rag/ -v -q

Expected: 3 passed (retriever tests), 12 skipped (router stubs + retriever stub), 0 failed.

Smoke test build_context_prompt end-to-end in mock mode:
  cd /c/Users/zezis/Desktop/code/uhr/uhr-ai/backend && python -c "
from services.rag.retriever import build_context_prompt
system, sources = build_context_prompt('Qual foi meu HbA1c?', 'test-user-id')
print('type:', type(system))
print('sources:', sources)
assert isinstance(system, str)
assert isinstance(sources, list)
print('signature OK')
"
</verification>

<success_criteria>
- retriever.py refactored to use router (route_query, execute_sql_steps, format_sql_block, assemble_chat_block)
- SYSTEM_PROMPT updated with D-18/D-19 instructions (structured data ground truth, synthesize across sections)
- build_context_prompt(question, user_id) -> tuple[str, list[str]] signature unchanged
- Existing 3 retriever tests green; 12 stubs still skipped; 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/05-query-routing-and-context-assembly/05-04-SUMMARY.md`
</output>
