---
phase: 05-query-routing-and-context-assembly
plan: 05
type: execute
wave: 3
depends_on:
  - 05-04
files_modified:
  - backend/tests/rag/test_router.py
  - backend/tests/rag/test_retriever.py
autonomous: true
requirements:
  - RETRIEVE-04
  - RETRIEVE-05

must_haves:
  truths:
    - "All 11 router test stubs unskipped and implemented with assertions"
    - "test_context_includes_sql_section unskipped and implemented"
    - "cd backend && python -m pytest tests/rag/ -q exits 0 with all tests passing"
    - "test_route_mock_mode confirms mock guard returns search_only without calling generate_json"
    - "test_sql_user_isolation confirms user_id is always injected by code (not LLM)"
    - "test_expand_abbreviations confirms HbA1c -> hemoglobina glicada transformation"
    - "test_chat_block_fallback confirms excerpt returned when structured_result unavailable"
  artifacts:
    - path: "backend/tests/rag/test_router.py"
      provides: "11 implemented test cases for router.py"
      contains: "assert"
    - path: "backend/tests/rag/test_retriever.py"
      provides: "1 implemented test for SQL section in context"
      contains: "test_context_includes_sql_section"
  key_links:
    - from: "backend/tests/rag/test_router.py"
      to: "services.rag.router"
      via: "direct import at top of file (module now exists)"
      pattern: "from services.rag.router import"
---

<objective>
Unskip and implement all 12 Wave 0 test stubs (11 in test_router.py + 1 in test_retriever.py). Run the full test suite to green.

Purpose: Validate that RETRIEVE-04 (routing) and RETRIEVE-05 (context assembly) are correctly implemented across the full test contract established in Wave 0.
Output: All 15 rag tests pass (3 original retriever + 11 router + 1 new retriever).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-query-routing-and-context-assembly/05-CONTEXT.md
@.planning/phases/05-query-routing-and-context-assembly/05-04-SUMMARY.md

<interfaces>
From backend/services/rag/router.py (implemented in Plans 02/03):
  expand_abbreviations(text: str) -> str
    "HbA1c" -> "hemoglobina glicada", "PA" -> "pressão arterial", etc.

  RoutingResult(BaseModel):
    intent: Literal["sql_only", "search_only", "mixed"]
    entities: list[str] = []
    time_range: list[str] | None = None
    sql_steps: list[str] = []
    search_queries: list[str] = []

  route_query(question: str, user_id: str) -> RoutingResult
    Mock mode (USE_MOCK_AZURE=true): returns RoutingResult(intent="search_only", search_queries=[expanded_question])
    On LLM failure: falls back to RoutingResult(intent="search_only", ...)

  execute_sql_steps(sql_steps: list[str], user_id: str) -> list[dict]
    Each dict: {"step": str, "table": str, "rows": list}
    user_id always injected by code via .eq("user_id", user_id)

  assemble_chat_block(doc_id: str, source_name: str, excerpt: str, collection_date: str | None) -> str
    Returns string starting with "[Fonte: ...]"
    Fallback to excerpt when structured_result unavailable
    Max 1200 chars

From backend/services/rag/retriever.py (refactored in Plan 04):
  build_context_prompt(question: str, user_id: str) -> tuple[str, list[str]]
  SYSTEM_PROMPT contains "DADOS ESTRUTURADOS DO PACIENTE"

Testing patterns from Phase 4 (test_retriever.py, test_context_block.py):
  - unittest.mock.patch for mocking external dependencies
  - patch("services.rag.router.generate_json") to control LLM output
  - patch("services.rag.router._get_client") to control Supabase calls
  - patch("services.rag.router._fetch_structured_result") for assembler tests
  - Environment variable overrides: patch.dict(os.environ, {"USE_MOCK_AZURE": "false"})
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement all 11 test bodies in test_router.py</name>
  <files>backend/tests/rag/test_router.py</files>
  <read_first>
    - backend/tests/rag/test_router.py (MANDATORY — read current stubs before implementing)
    - backend/services/rag/router.py (read full implementation to understand what to assert)
    - backend/tests/rag/test_retriever.py (mock pattern reference — patch, MagicMock usage)
    - .planning/phases/05-query-routing-and-context-assembly/05-VALIDATION.md (per-task verification map)
  </read_first>
  <action>
Read backend/tests/rag/test_router.py current content (11 skipped stubs).
Read backend/services/rag/router.py to understand the actual implementation.

Replace the file content with fully implemented tests. Keep the same sys.path setup at the top. Add module-level imports now that router.py exists:
  import pytest
  import os
  from unittest.mock import patch, MagicMock
  from services.rag.router import (
      expand_abbreviations, route_query, RoutingResult,
      execute_sql_steps, format_sql_block, assemble_chat_block,
  )

Implement each test as follows (remove @pytest.mark.skip, add assertions):

test_expand_abbreviations:
  - Call expand_abbreviations("HbA1c e PA estao altos")
  - Assert "hemoglobina glicada" in result
  - Assert "pressão arterial" in result
  - Assert "HbA1c" not in result (replaced)
  - Also assert expand_abbreviations("normal text") == "normal text" (no change for non-abbreviations)

test_route_mock_mode:
  - Ensure mock mode is active: patch.dict(os.environ, {"USE_MOCK_AZURE": "true"})
  - Also reset _use_mock_cache: patch("services.rag.router._use_mock_cache", None)
  - Call route_query("Qual meu HbA1c?", "user-1")
  - Assert result.intent == "search_only"
  - Assert isinstance(result, RoutingResult)
  - Assert generate_json was NOT called: patch "services.rag.router.generate_json" as mock_gen; assert mock_gen.call_count == 0

test_route_narrative_returns_search_only:
  - Set mock Azure OFF: patch.dict(os.environ, {"USE_MOCK_AZURE": "false"})
  - patch("services.rag.router._use_mock_cache", False)
  - patch "services.rag.router.generate_json" to return: '{"intent": "search_only", "entities": ["cardiologista"], "time_range": null, "sql_steps": [], "search_queries": ["o que meu cardiologista disse"]}'
  - Call route_query("O que meu cardiologista disse?", "user-1")
  - Assert result.intent == "search_only"
  - Assert "cardiologista" in result.entities

test_route_aggregation_returns_sql_only:
  - Set mock Azure OFF: patch.dict(os.environ, {"USE_MOCK_AZURE": "false"})
  - patch("services.rag.router._use_mock_cache", False)
  - patch "services.rag.router.generate_json" to return: '{"intent": "sql_only", "entities": ["hemoglobina glicada"], "time_range": ["2024-01-01", "2024-12-31"], "sql_steps": ["observations:hemoglobina glicada:range:2024-01-01:2024-12-31"], "search_queries": []}'
  - Call route_query("Qual meu HbA1c medio no ano passado?", "user-1")
  - Assert result.intent == "sql_only"
  - Assert len(result.sql_steps) > 0
  - Assert "hemoglobina glicada" in result.sql_steps[0]

test_route_mixed_intent:
  - Set mock Azure OFF, patch _use_mock_cache False
  - patch generate_json to return: '{"intent": "mixed", "entities": ["medicamentos"], "time_range": null, "sql_steps": ["medications:active"], "search_queries": ["o que meu cardiologista disse sobre medicamentos"]}'
  - Call route_query("Minhas medicacoes e o que meu cardiologista disse?", "user-1")
  - Assert result.intent == "mixed"
  - Assert "medications:active" in result.sql_steps
  - Assert len(result.search_queries) > 0

test_route_fallback_on_llm_failure:
  - Set mock Azure OFF, patch _use_mock_cache False
  - patch "services.rag.router.generate_json" to raise Exception("LLM error")
  - Call route_query("Qualquer pergunta", "user-1")
  - Assert result.intent == "search_only" (soft-fail D-04)
  - Assert isinstance(result, RoutingResult)

test_route_returns_routing_result_model:
  - Call route_query("test", "u1") in mock mode
  - Assert isinstance(result, RoutingResult)
  - Assert hasattr(result, "intent")
  - Assert hasattr(result, "sql_steps")
  - Assert hasattr(result, "search_queries")

test_sql_observations_scoped_by_user_id:
  - patch "services.rag.router._get_client" to return a MagicMock
  - Set up mock: mock_client = MagicMock(); mock_resp = MagicMock(); mock_resp.data = [{"normalized_analyte": "hemoglobina glicada", "value_str": "7.8", "unit": "%", "flag": "high", "observed_date": "2024-01-15"}]; mock_client.table.return_value.select.return_value.eq.return_value.eq.return_value.order.return_value.limit.return_value.execute.return_value = mock_resp
  - Call execute_sql_steps(["observations:hemoglobina glicada"], "user-123")
  - Assert len(results) == 1
  - Assert results[0]["table"] == "patient_observations"
  - Assert len(results[0]["rows"]) == 1

test_sql_user_isolation:
  - Call execute_sql_steps(["medications:active"], "user-A") with mocked _get_client
  - Capture the mock call chain
  - Assert .eq("user_id", "user-A") was called (user_id injected by code, not LLM)
  - Approach: MagicMock records all chained calls; check that eq was called with "user_id" argument
  - Simpler approach: patch _get_client, capture mock_client.table.return_value... and assert the eq("user_id", "user-A") call appears in mock_client.method_calls or the chain

test_chat_block_structured_lab:
  - patch "services.rag.router._fetch_structured_result" to return a StructuredResult with:
    document_family="structured_lab"
    structured_data={"summary": "Controle glicemico subotimo", "findings": [{"name": "HbA1c", "value": "7.8", "unit": "%", "flag": "high"}], "entities_for_memory": ["diabetes mellitus"], "collection_date": "2024-01-15"}
  - Call assemble_chat_block("doc-1", "Hemograma", "raw excerpt", "2024-01-15")
  - Assert "[Fonte: Hemograma" in result
  - Assert "HbA1c" in result
  - Assert "7.8" in result
  - Assert "[high]" in result
  - Assert "Summary:" in result

test_chat_block_fallback:
  - patch "_fetch_structured_result" to return None
  - Call assemble_chat_block("doc-1", "Lab Test", "this is the raw excerpt", "2024-01-15")
  - Assert "[Fonte: Lab Test" in result
  - Assert "raw excerpt" in result (fallback excerpt used)
  - Assert len(result) <= 1200 (budget cap)
  </action>
  <verify>
    <automated>cd /c/Users/zezis/Desktop/code/uhr/uhr-ai/backend && python -m pytest tests/rag/test_router.py -v -q 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - Running "python -m pytest tests/rag/test_router.py -v" exits 0
    - Output shows "11 passed" (all stubs now implemented and passing)
    - Zero skipped, zero failed in test_router.py
    - test_expand_abbreviations asserts "hemoglobina glicada" in result (grep for "hemoglobina glicada" in test file)
    - test_route_mock_mode asserts result.intent == "search_only" (grep for "search_only" in test file)
    - test_chat_block_fallback asserts fallback text present when _fetch_structured_result returns None
    - No @pytest.mark.skip decorator remains in the file (grep — must be absent)
  </acceptance_criteria>
  <done>All 11 router tests pass. No stubs remain. test_router.py exits 0 with 11 passed.</done>
</task>

<task type="auto">
  <name>Task 2: Implement test_context_includes_sql_section and run full suite</name>
  <files>backend/tests/rag/test_retriever.py</files>
  <read_first>
    - backend/tests/rag/test_retriever.py (MANDATORY — read current content before modifying)
    - backend/services/rag/retriever.py (refactored content from Plan 04 — understand what to assert)
    - backend/tests/rag/test_router.py (mock pattern reference)
  </read_first>
  <action>
Read backend/tests/rag/test_retriever.py current content (3 green tests + 1 skipped stub).

Replace the skipped test_context_includes_sql_section stub with a full implementation:

Remove the @pytest.mark.skip decorator. Implement the body:
  - Set mock Azure OFF so route_query makes a "real" (mocked) LLM call: patch.dict(os.environ, {"USE_MOCK_AZURE": "false"})
  - patch "services.rag.retriever.route_query" directly to return RoutingResult(intent="sql_only", sql_steps=["medications:active"], search_queries=[])
  - patch "services.rag.retriever.execute_sql_steps" to return [{"step": "medications:active", "table": "patient_medications", "rows": [{"raw_medication": "Metformina", "dose": "850mg", "frequency": "2x ao dia"}]}]
  - patch "services.rag.retriever.format_sql_block" to return "=== DADOS ESTRUTURADOS DO PACIENTE ===\nMedicamento: Metformina 850mg\n=== FIM DOS DADOS ESTRUTURADOS ==="
  - patch "services.rag.retriever.generate_embedding" to return None
  - patch "services.rag.retriever.search" to return []  (sql_only intent, no search)
  - Call build_context_prompt("Quais sao minhas medicacoes ativas?", "user-1")
  - Assert "DADOS ESTRUTURADOS DO PACIENTE" in system_prompt (SQL section present)
  - Assert "Metformina" in system_prompt (actual data present)
  - Assert isinstance(sources, list)

Also add this import at the top if not already present (after existing imports):
  from services.rag.router import RoutingResult

Note: import RoutingResult inside the test function body if module-level import causes issues with mock patching.
  </action>
  <verify>
    <automated>cd /c/Users/zezis/Desktop/code/uhr/uhr-ai/backend && python -m pytest tests/rag/ -v -q 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - Running "python -m pytest tests/rag/ -v" exits 0
    - Output shows "15 passed, 0 skipped, 0 failed" (or "15 passed" total across test_router.py + test_retriever.py + test_context_block.py)
    - test_context_includes_sql_section shows PASSED (not SKIPPED)
    - No @pytest.mark.skip remains in test_retriever.py for test_context_includes_sql_section (grep-verifiable)
    - test_context_includes_sql_section asserts "DADOS ESTRUTURADOS DO PACIENTE" in system prompt (grep for "DADOS ESTRUTURADOS" in test_retriever.py)
    - All 3 original retriever tests (test_build_context_prompt_calls_generate_embedding, test_build_context_prompt_passes_vector_to_search, test_retriever_degrades_when_embedding_fails) still pass
    - test_context_block.py tests still pass (context_block.py was not modified)
  </acceptance_criteria>
  <done>Full rag test suite green. 15 tests pass, 0 skipped, 0 failed. Phase 5 automated validation complete.</done>
</task>

</tasks>

<verification>
Final suite check:
  cd /c/Users/zezis/Desktop/code/uhr/uhr-ai/backend && python -m pytest tests/ -q 2>&1

Expected: all tests pass (test_router.py: 11, test_retriever.py: 4, test_context_block.py: 7 = 22 total), 0 failed.

Per VALIDATION.md sampling:
  cd /c/Users/zezis/Desktop/code/uhr/uhr-ai/backend && python -m pytest tests/rag/test_router.py::test_route_aggregation_returns_sql_only tests/rag/test_router.py::test_route_mock_mode tests/rag/test_router.py::test_chat_block_fallback -v
</verification>

<success_criteria>
- All 11 router test stubs unskipped and implemented with behavioral assertions
- test_context_includes_sql_section unskipped and passing
- cd backend && python -m pytest tests/ -q exits 0
- No @pytest.mark.skip decorators remain in either test file
- RETRIEVE-04: test_route_aggregation_returns_sql_only, test_route_narrative_returns_search_only, test_route_mixed_intent, test_route_fallback_on_llm_failure, test_route_mock_mode all green
- RETRIEVE-05: test_chat_block_structured_lab, test_chat_block_fallback, test_context_includes_sql_section all green
</success_criteria>

<output>
After completion, create `.planning/phases/05-query-routing-and-context-assembly/05-05-SUMMARY.md`
</output>
