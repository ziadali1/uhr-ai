---
phase: 05-query-routing-and-context-assembly
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/tests/rag/test_router.py
  - backend/tests/rag/test_retriever.py
autonomous: true
requirements:
  - RETRIEVE-04
  - RETRIEVE-05

must_haves:
  truths:
    - "pytest collects test_router.py without ImportError"
    - "All 11 router stubs are skipped (not failed) when pytest runs"
    - "The new retriever stub is skipped (not failed) when pytest runs"
    - "Existing 3 test_retriever.py tests remain green"
  artifacts:
    - path: "backend/tests/rag/test_router.py"
      provides: "Wave 0 stubs for all RETRIEVE-04 and RETRIEVE-05 test cases"
      contains: "@pytest.mark.skip"
    - path: "backend/tests/rag/test_retriever.py"
      provides: "New stub test_context_includes_sql_section"
      contains: "test_context_includes_sql_section"
  key_links:
    - from: "backend/tests/rag/test_router.py"
      to: "services.rag.router"
      via: "import inside test body (deferred import pattern — module does not exist yet)"
      pattern: "import inside.*def test_"
---

<objective>
Create Wave 0 test stubs for all Phase 5 test cases. All stubs are marked `@pytest.mark.skip` so pytest collects them without error even though the modules under test do not yet exist.

Purpose: Establish the full test contract before any implementation. Executors in Wave 1–3 will unskip and implement tests as they build the corresponding code.
Output: `backend/tests/rag/test_router.py` (11 stubs), `backend/tests/rag/test_retriever.py` (1 new stub appended)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-query-routing-and-context-assembly/05-CONTEXT.md
@.planning/phases/05-query-routing-and-context-assembly/05-VALIDATION.md

<interfaces>
<!-- Existing test infrastructure (Phase 4 established) -->
<!-- backend/conftest.py already exists — no new conftest needed -->

From backend/tests/rag/test_retriever.py (existing, must remain green):
```python
# Existing pattern — imports inside function body, sys.path manipulation at top
import sys, os
sys.path.insert(0, os.path.join(os.path.dirname(__file__), "../.."))
from unittest.mock import patch, MagicMock
from services.rag.retriever import build_context_prompt
```

From backend/tests/rag/test_context_block.py (Phase 4 Wave 0 pattern to replicate):
```python
@pytest.mark.skip(reason="Wave 0 stub — implement after router.py created")
def test_expand_abbreviations():
    pass
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test_router.py with 11 Wave 0 stubs</name>
  <files>backend/tests/rag/test_router.py</files>
  <read_first>
    - backend/tests/rag/test_retriever.py (import pattern and sys.path setup to replicate)
    - backend/tests/rag/test_context_block.py (Wave 0 skip pattern from Phase 4)
    - backend/conftest.py (verify it exists — no new conftest needed)
    - .planning/phases/05-query-routing-and-context-assembly/05-VALIDATION.md (exact test function names)
  </read_first>
  <action>
Create `backend/tests/rag/test_router.py` as a new file. This file must:

1. Start with sys.path setup (same pattern as test_retriever.py):
```python
"""Tests for RETRIEVE-04 and RETRIEVE-05 — query routing and context assembly (Phase 5)."""
import sys
import os
sys.path.insert(0, os.path.join(os.path.dirname(__file__), "../.."))

import pytest
```

2. Include exactly these 11 stub functions, all decorated `@pytest.mark.skip(reason="Wave 0 stub — implement after router.py created")`:

- `test_route_aggregation_returns_sql_only` — stub body: `pass`
- `test_route_narrative_returns_search_only` — stub body: `pass`
- `test_route_mixed_intent` — stub body: `pass`
- `test_route_fallback_on_llm_failure` — stub body: `pass`
- `test_route_mock_mode` — stub body: `pass`
- `test_expand_abbreviations` — stub body: `pass`
- `test_sql_observations_scoped_by_user_id` — stub body: `pass`
- `test_sql_user_isolation` — stub body: `pass`
- `test_chat_block_structured_lab` — stub body: `pass`
- `test_chat_block_fallback` — stub body: `pass`
- `test_route_returns_routing_result_model` — stub body: `pass`

Each stub uses import-inside-body pattern to avoid ImportError when module doesn't exist:
```python
@pytest.mark.skip(reason="Wave 0 stub — implement after router.py created")
def test_route_aggregation_returns_sql_only():
    # import inside body: router module does not exist yet
    pass
```

No module-level imports from `services.rag.router` — that module doesn't exist yet.
  </action>
  <verify>
    <automated>cd /c/Users/zezis/Desktop/code/uhr/uhr-ai/backend && python -m pytest tests/rag/test_router.py -v 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `backend/tests/rag/test_router.py` exists
    - Running `python -m pytest tests/rag/test_router.py -v` exits 0 (all skipped, none failed)
    - Output contains "11 skipped" (or each stub listed as "SKIPPED")
    - File contains exactly 11 functions matching: `test_route_aggregation_returns_sql_only`, `test_route_narrative_returns_search_only`, `test_route_mixed_intent`, `test_route_fallback_on_llm_failure`, `test_route_mock_mode`, `test_expand_abbreviations`, `test_sql_observations_scoped_by_user_id`, `test_sql_user_isolation`, `test_chat_block_structured_lab`, `test_chat_block_fallback`, `test_route_returns_routing_result_model`
    - File contains `@pytest.mark.skip` (grep-verifiable)
    - No module-level import of `services.rag.router` (grep for `^from services.rag.router` or `^import services.rag.router` — must be absent)
  </acceptance_criteria>
  <done>pytest collects test_router.py, reports 11 skipped, 0 failed, 0 errors</done>
</task>

<task type="auto">
  <name>Task 2: Append test_context_includes_sql_section stub to test_retriever.py</name>
  <files>backend/tests/rag/test_retriever.py</files>
  <read_first>
    - backend/tests/rag/test_retriever.py (read current content before modifying — MANDATORY)
    - .planning/phases/05-query-routing-and-context-assembly/05-VALIDATION.md (stub name)
  </read_first>
  <action>
Read the current content of `backend/tests/rag/test_retriever.py` (it has 3 existing tests that must remain unchanged). Append exactly this stub at the end of the file:

```python


@pytest.mark.skip(reason="Wave 0 stub — implement after retriever.py refactored in Plan 05-04")
def test_context_includes_sql_section():
    # Verifies that build_context_prompt returns context containing SQL structured block
    # when route_query returns sql_only or mixed intent
    pass
```

Also add `import pytest` at the top of the file if not already present (check before adding — do not duplicate).

Preserve ALL existing test functions exactly as-is. Do not modify any existing line.
  </action>
  <verify>
    <automated>cd /c/Users/zezis/Desktop/code/uhr/uhr-ai/backend && python -m pytest tests/rag/test_retriever.py -v 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `backend/tests/rag/test_retriever.py` contains `def test_context_includes_sql_section`
    - File contains `@pytest.mark.skip` (grep-verifiable on the new stub)
    - Running `python -m pytest tests/rag/test_retriever.py -v` shows 3 PASSED + 1 SKIPPED, 0 FAILED
    - The 3 existing functions (`test_build_context_prompt_calls_generate_embedding`, `test_build_context_prompt_passes_vector_to_search`, `test_retriever_degrades_when_embedding_fails`) are unchanged
  </acceptance_criteria>
  <done>test_retriever.py has 4 tests: 3 green + 1 skipped. Full rag suite: 0 failures.</done>
</task>

</tasks>

<verification>
Run the full rag test suite after both tasks:
```
cd backend && python -m pytest tests/rag/ -v -q
```
Expected: 3 passed, 12 skipped (11 router + 1 retriever), 0 failed, 0 errors.
</verification>

<success_criteria>
- `backend/tests/rag/test_router.py` exists with 11 skipped stubs covering all RETRIEVE-04/RETRIEVE-05 test cases
- `backend/tests/rag/test_retriever.py` has 1 new skipped stub appended, 3 existing tests still green
- `cd backend && python -m pytest tests/rag/ -q` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/05-query-routing-and-context-assembly/05-01-SUMMARY.md`
</output>
